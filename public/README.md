# Public Assets Folder

Place your profile photo here.

## Profile Photo

1. Add your profile photo image file to this folder
2. Name it `profile-photo.jpg` (or update the filename in `components/sections/Hero.tsx` if using a different name)
3. Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`

The image will be automatically displayed in the Hero section avatar.

